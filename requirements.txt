# requirements.txt — sv3 (persistencia + enriquecimiento Sigrid)
# Paquete común: pip install -e ../comun (local) / Dockerfile (build)
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.2
httpx>=0.27
sqlalchemy>=2.0.30
psycopg[binary]>=3.1
pypdf>=4.2
# Render del contrato .docx → PDF (docx_pdf_renderer):
mammoth>=1.7
xhtml2pdf>=0.2.15
# Conversión del contrato a Markdown (markitdown) para enviarlo a la IA:
markitdown[pdf,docx]>=0.1
# Solo para los scripts de diagnóstico (no entra en la imagen):
python-dotenv>=1.0
